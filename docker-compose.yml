version: '3.1'
services:
  jenkins:
    image: uscdev/jenkins
    ports:
      - 50000:50000
    environment:
      - JAVA_OPTS=-Duser.timezone=${TZ}
    networks:
      web-bus:
    volumes:
      - ${USC_LOCAL_DIR}/jenkins:/var/jenkins_home
    secrets:
      - ca.pem
      - cert.pem
      - key.pem
    deploy:
      replicas: 1
      labels:
        - com.df.notify=true
        - com.df.distribute=true
        - com.df.serviceDomain=jenkins.docker.usc.edu
        - com.df.port=8080
        - com.df.httpsOnly=true
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.hostname==${NODE01}
networks:
  web-bus:
    external:
      name: web-bus
secrets:
  ca.pem:
    external: true
  cert.pem:
    external: true
  key.pem:
    external: true

